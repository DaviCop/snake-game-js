{
    "post-install-cmd": [
        "php artisan clear-compiled",
        "chmod -R 777 ./public",
        "chmod -R 777 ./views",
        "chmod -R 777 ./views/pages",
        "chmod -R 777 ./views/partials",
        "chmod -R 777 ./vendor",
        "chmod -R 777 ./"

    ],
    "require": {
        "php": "~7"
    }
    
}

